---
- name: Create merged list
  set_fact:
    merged_hosts: "{{ merged_hosts + [item] }}"
  with_items:
    - "{{ workers }}"
    - "{{ controllers }}"
    - "{{ load_balancer }}"

- name: Add hosts into system hosts file
  blockinfile:
    path: /etc/hosts
    block: |
      {{ item.ip }} {{ item.name }}
    marker: "# {mark} ANSIBLE MANAGED BLOCK {{ item.name }}"
  loop:
    "{{ merged_hosts }}"
  become: true
  when: mao is defined

- name: Vagrant ssh key
  shell: vagrant ssh-config node-1 | grep IdentityFile | awk '{print $2}'
  args:
    chdir: ../vagrant
  register: vagrant_ssh_key

- name: Add hosts to ansible host file
  template:
    src: hosts.j2
    dest: "{{ inventory_dir }}/vagrant_hosts"
  loop:
    "{{ merged_hosts }}"